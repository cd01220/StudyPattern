tag history.
tag1: 
    1 Reactor base function is ready.
    2 For now, Reactor and EventHandler's design are same as ACE.  In order to delete the dependence of EventHandler -> Reactor, we must changes the design.  We create this tag before we change the design.